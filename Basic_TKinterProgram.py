from tkinter import *  # importing tkinter
 
root = Tk()       #creating a root window

label = Label(root,text = "Helo world! ")         # Adding label by using Label function

label.pack()      # we have to display that label in GUI app right? So pack function used for it.

root.mainloop() # In GUI apps everything runs in the loop. So here, we are creating a loop for our root window,









